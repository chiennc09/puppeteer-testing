{
  "name": "puppeteer-testing",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha tests/*.test.js --reporter mochawesome --timeout 60000",
    "test:login": "mocha tests/login.test.js --reporter mochawesome --timeout 60000",
    "test:cart": "mocha tests/cart.test.js --reporter mochawesome --timeout 60000",
    "test:register": "mocha tests/register.test.js --reporter mochawesome --timeout 60000",
    "test:search": "mocha tests/search.test.js --reporter mochawesome --timeout 60000",
    "test:order": "mocha tests/order.test.js --reporter mochawesome --timeout 60000",
    "test:payment": "mocha tests/payment.test.js --reporter mochawesome --timeout 60000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "chai": "^5.2.0",
    "mocha": "^11.5.0",
    "mochawesome": "^7.1.3",
    "puppeteer": "^24.10.0"
  }
}
